# ملاحظات تنفيذ وإختبار 2026-09-07

## التعديلات المنفذة
تم تحديث سعر التسجيل إلى 35 دولارًا بدل 79 دولارًا، وتحديث التقسيط إلى 17.5$ + 17.5$، وإضافة إبراز واضح لملفات PDF المرافقة لكل درس داخل المسارات وقسم المزايا.

تم إنشاء صفحة `/workshop` بنفس هوية الصفحة: خلفية فاتحة دافئة، كحلي داكن، أخضر تركوازي، ولمسات ذهبية. الصفحة تجمع الاسم والبريد ورقم WhatsApp والمادة التي يدرّسها المتقدم اختياريًا.

تم إنشاء `/api/workshop` للتحقق من البيانات وحفظها في Prisma، وإضافة نموذج `WorkshopRegistration` إلى مخطط قاعدة البيانات.

## التحقق البصري
صفحة `/` تعرض رابط الورشة في شريط التنقل والهيرو، والسعر الحالي 35$ مقابل 79$، والتقسيط 17.5$ + 17.5$.

صفحة `/workshop` تعرض نموذج التسجيل بشكل واضح في بطاقة بيضاء، مع شرح لما سيخرج به المتدرب ورسالة خصوصية مختصرة.

## التحقق البرمجي
نجح `prisma generate`، ونجح `npm run lint`، ونجح `npm run build`. ظهرت مسارات `/workshop` و`/api/workshop` ضمن مخرجات البناء.

## ملاحظة تشغيلية
حتى يعمل حفظ التسجيلات في بيئة الإنتاج، يجب أن يكون `DATABASE_URL` في Vercel متصلًا بقاعدة بيانات إنتاجية قابلة للكتابة، ثم تشغيل ترحيل Prisma المناسب. لا ينبغي الاعتماد على SQLite محليًا كحل إنتاجي دائم على Vercel.

## حالة النشر العام
تم رفع commit `5f8949f` بنجاح إلى `main` في GitHub. الموقع العام `course.manasadigital.com` كان لا يزال يعرض النسخة السابقة وقت التحقق، بما فيها 30$ مقابل 49$؛ لذلك لم أعتبر النشر العام مكتملًا. لوحة Vercel أعادت صفحة تسجيل الدخول، ولا توجد جلسة دخول متاحة لمتابعة deployment يدويًا. الكود الجديد والـ build المحلي صحيحان، لكن يلزم تشغيل deployment من Vercel أو ربط webhook/التأكد من أن المشروع المنشور يستخدم مستودع `fhrrutedf/notebooklm-course-landing` وفرع `main`.
